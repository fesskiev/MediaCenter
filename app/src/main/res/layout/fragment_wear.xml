<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary"
    android:orientation="vertical"
    android:padding="@dimen/activity_horizontal_margin">

    <TextView
        android:id="@+id/connectionStatusText"
        style="@style/AboutTextAppearance"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:drawableTop="@drawable/icon_wear"
        android:gravity="center"
        tools:text="You have no Wear devices linked to your phone at this time" />

    <View
        android:id="@+id/divider"
        style="@style/DividerStyle"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="@dimen/activity_horizontal_margin" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/installAppButton"
            style="@style/ButtonStyle"
            android:layout_width="@dimen/button_width"
            android:layout_height="@dimen/button_height"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/wear_install"
            android:visibility="invisible" />

        <Button
            android:id="@+id/openAppButton"
            style="@style/ButtonStyle"
            android:layout_width="@dimen/button_width"
            android:layout_height="@dimen/button_height"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/wear_open"
            android:visibility="invisible" />

    </LinearLayout>

</LinearLayout>