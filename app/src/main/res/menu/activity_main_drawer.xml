<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group
        android:id="@+id/menu_content">
        <item android:title="@string/drawer_title_content">
            <menu>
                <group android:checkableBehavior="single">
                    <item
                        android:id="@+id/audio_content"
                        android:icon="@drawable/item_audio_icon"
                        android:title="@string/drawer_item_audio" />

                    <item
                        android:id="@+id/video_content"
                        android:icon="@drawable/item_video_icon"
                        android:title="@string/drawer_item_video" />
                </group>

            </menu>
        </item>
    </group>

    <group
        android:id="@+id/menu_converter"
        android:checkableBehavior="single">
        <item
            android:id="@+id/converter"
            android:icon="@drawable/icon_converter"
            android:title="@string/drawer_item_converter"/>
    </group>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/playlist"
            android:icon="@drawable/item_playlist_icon"
            android:title="@string/drawer_item_playlist" />
    </group>

    <group
        android:id="@+id/menu_settings"
        android:checkableBehavior="single">

        <item
            android:id="@+id/settings"
            android:icon="@drawable/icon_settings"
            android:title="@string/drawer_item_settings" />
    </group>

    <group
        android:id="@+id/menu_about"
        android:checkableBehavior="single">

        <item
            android:id="@+id/about"
            android:icon="@drawable/icon_about"
            android:title="@string/drawer_item_about" />
    </group>

    <group
        android:id="@+id/menu_billing"
        android:checkableBehavior="single">

        <item
            android:id="@+id/billing"
            android:title="@string/drawer_remove_banners"
            android:icon="@drawable/icon_remove_banners"/>
    </group>

</menu>